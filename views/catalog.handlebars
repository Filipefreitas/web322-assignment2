<section>
    <div class="container all-catalog grid grid-col-6">
        {{#each products}}
            <div class="movie-card">
                <a href="/catalog/{{this.id}}">                    
                    <img src = "/img/posters/{{this.src}}" alt = "{{this.alt}}">
                </a>
            </div>
        {{/each}}
    </div>        
</section>
